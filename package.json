{
  "name": "lab-experiment-tracking",
  "version": "1.0.0",
  "private": true,
  "description": "Data model for a laboratory experiment tracking system (Postgres + Prisma).",
  "license": "MIT",
  "scripts": {
    "prisma:generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "prisma db seed",
    "studio": "prisma studio",
    "db:setup": "prisma migrate deploy && prisma db seed"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^6.3.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "@types/node": "^22.10.7"
  },
  "dependencies": {
    "@prisma/client": "^6.3.0"
  }
}
