# Copy to .env for running Prisma commands from the host (e.g. migrate dev, studio).
# Inside Docker Compose these values are provided by docker-compose.yml instead.
#
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://lab:lab@localhost:5432/lab?schema=public"
